// @flow

import type { SetPage } from '../types';

export default ([
  {
    name: 'Collections',
    sets: [
      {
        name: 'Collections',
        big: true,
        query: 'special:tempCollections',
        sections: []
      }
    ]
  }
]: SetPage);
