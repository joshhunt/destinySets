// @flow

import type { SetPage } from '../types';

export default ([
  {
    name: 'Collections',
    sets: [
      {
        name: 'Collections',
        id: 'COLLECTIONS_COLLECTIONS',
        big: true,
        query: 'special:tempCollections',
        sections: []
      }
    ]
  }
]: SetPage);
