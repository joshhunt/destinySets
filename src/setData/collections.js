// @flow

import type { SetPage } from '../types';

export default ([
  {
    name: 'Collections',
    id: 'COLLECTIONS',
    sets: [
      {
        name: 'Collections',
        big: true,
        query: 'special:tempCollections',
        sections: []
      }
    ]
  }
]: SetPage);
